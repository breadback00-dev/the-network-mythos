import{test,expect}from'@playwright/test';for(const path of['/','/adventure/'])test('static scene '+path,async({page})=>{await page.goto(path);await expect(page.getByRole('heading',{name:'The Archive',exact:true})).toBeVisible();await page.getByRole('button',{name:'Porchlight',exact:true}).click();await expect(page.getByRole('heading',{name:'Porchlight',exact:true})).toBeVisible();});test('data retry',async({page})=>{await page.route('**/content/mara/scenes.json',r=>r.abort());await page.goto('/');await expect(page.getByRole('button',{name:'Retry loading'})).toBeVisible();await page.unroute('**/content/mara/scenes.json');await page.getByRole('button',{name:'Retry loading'}).click();await expect(page.getByRole('heading',{name:'The Archive',exact:true})).toBeVisible();});
