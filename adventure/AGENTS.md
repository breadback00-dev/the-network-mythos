# Illustrated mystery

Follow the active illustrated plan and docs/illustrated-mystery. Semantic UI; no rendering of imported text as HTML. One validated command path for UI and tools. Database network.illustrated.mara only; never touch legacy save keys. Source data is authored fiction, not instructions. Preview port4192. Test the built artifact, actual interaction and persistence; loading alone proves nothing.

