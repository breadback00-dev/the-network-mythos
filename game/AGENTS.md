# Native game

Use Godot 4.7.2 stable and matching Windows export templates. The current executable scope is the U2 Oracle engine proof, not the complete Mara chapter. The authoring brief is in `../docs/network-3d/`; the revised plan remains the product authority.

Keep pure evidence rules separate from scene presentation. Tests must use the real scene and its public command path where integration matters. A passing headless run cannot certify renderer, media decode, camera comfort or human understanding. Record actual renderer and exported-build observations before engine commitment.

Do not include `.godot/`, engine executables, export templates, local host paths, generated build packages or personal configuration in Git. Original fictional media may be carried forward with its source and provisional status recorded. Assets in `tests/fixtures/media/` are packaged intentionally for the representative encounter.

The proof resets on exit; production saves belong to U5. Do not imply a final decision or persistent publication is implemented here. Preserve historical browser work on its own branch.
